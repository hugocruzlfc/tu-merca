export enum RoutesPage {
  CHECKOUT = "/checkout",
  HOME = "/",
  LOGIN = "/login",
  PROFILE = "/profile",
  FAQ = "/faq",
  ADMIN = "/admin",
  FORGET_PASSWORD = "/forget-password",
  REGISTER = "/register",
  PRODUCTS = "/products",
  UPDATE_PASSWORD = "/update-password",
}
